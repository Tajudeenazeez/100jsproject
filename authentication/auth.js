//write auth to allow login user to download report for a specific year activity

const downloadActivityReport = (year) => {
    //retrieve the user id from the session
    try{
        fetch(userId).then(
            ()=>{}
        )
      
        let loggedIn = false
        if(loggedIn){
            
        }


    } catch(error) {
        console.log(error.message)
    }

}
